<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chicky_reviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chicky_Reviews">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Chicky_Reviews.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.newReview.NewReviewActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.main.newReview.ExtraActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.main.formattedReview.FormattedReview"
            android:screenOrientation="portrait" />
    </application>

</manifest>